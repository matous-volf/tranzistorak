---
name: Docker build check
on: # yamllint disable-line rule:truthy
  pull_request:
    types: [opened, reopened, synchronize]
permissions: {}
jobs:
  check:
    name: Docker build check
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Code checkout
        uses: https://github.com/actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - name: .env symlink creation
        run: ln -s .env.example .env

      - name: Check
        uses: https://codeberg.org/matous-volf/action-docker-build-check@c37e3a48da43849ce546fac1aa407134d08581e1 # v2.0.0
        with:
          compose_file: docker-compose.yaml
